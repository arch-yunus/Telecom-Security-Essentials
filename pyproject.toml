[project]
name = "telecom-security-essentials"
version = "0.1.0"
description = "A professional suite for telecom protocol (SS7/Diameter/GTP) security analysis."
authors = [
    { name = "Bahadir" } # Based on user path, change if necessary
]
dependencies = [
    "pyyaml>=6.0",
    "pydantic>=2.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
